Pod::Spec.new do |s|
  s.name     = 'multiCellPodSupport'
  s.version  = '1.0'
  s.license  = 'MIT'
  s.summary  = 'UITableViewCell 多功能菜单，滑动展现菜单'
  s.homepage = 'https://github.com/kingundertree/MultiFunctionCell'
  s.author   = { '@xiazer' => '@xiazer' }
  s.source   = { :git => 'https://github.com/kingundertree/MultiFunctionCell' }
  s.platform = :ios  
  s.source_files = ''
  s.resources = "A"
  s.framework = 'UIKit'

  s.requires_arc = true  
end